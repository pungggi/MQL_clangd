Analyze the MQL5 codebase for anti-patterns. Check for:
1. Magic numbers (replace with named constants)
2. Deep nesting (refactor to early returns)
3. Dead code (unused variables, unreachable branches)
4. Code duplication (extract to shared includes)
5. Naming issues (inconsistent conventions, shadowing)

For each finding, show file, line, issue, and suggested fix.
Group by severity. Ask before applying fixes.
